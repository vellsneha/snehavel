# SHA-256 hash of password for NDA protected design case studies (NFC, TASK, PULSE)
# Example (replace): echo -n "your-password-here" | shasum -a 256
VITE_NDA_ACCESS_PASSWORD_HASH=replace-with-sha256-hash
